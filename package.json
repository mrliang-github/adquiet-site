{
  "name": "adquiet-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:content": "node scripts/sync-published-content.mjs",
    "build": "node scripts/build-site.mjs",
    "test": "npm run build && node --test tests/*.test.mjs",
    "verify": "npm run sync:content && npm test"
  },
  "devDependencies": {
    "marked": "^18.0.7",
    "sanitize-html": "^2.17.6"
  }
}
